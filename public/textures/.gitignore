# Committed, unlike public/models. See the root .gitignore for why: the Moon
# maps are converted with `sips`, which is macOS-only, so fetching them is not
# reproducible off a Mac. `npm run textures:fetch --force` refreshes them.
